SELECT c.collection_id, cu.customer_name
FROM collections c
JOIN customers cu ON cu.customer_id = c.customer_id
WHERE c.collection_id = {{appsmith.URL.queryParams.collectionId ? Number(appsmith.URL.queryParams.collectionId) : 0}};